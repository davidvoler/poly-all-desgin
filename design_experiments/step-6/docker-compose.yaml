
services:
  nginx:
    image: nginx:alpine
    container_name: local_nginx_server
    ports:
      - "0.0.0.0:8081:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./:/usr/share/nginx/html
    restart: always