data/

# flutter_dotenv runtime config — copy from .env.example to assets/.env
# and edit (contains Auth0 client id, API base URLs, etc.).
**/assets/.env
.env

# Self-signed certs generated by deploy_secure/certs/generate-local.sh.
# The script is committed, the generated material isn't.
deploy_secure/certs/live/
deploy_secure/certs/options-ssl-nginx.conf
deploy_secure/certs/ssl-dhparams.pem