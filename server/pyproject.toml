[project]
name = "poly-server"
version = "0.1.0"
description = "Polyglots backend — FastAPI"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
]

[tool.uv]
dev-dependencies = [
    "httpx>=0.27",  # for tests / quick smoke checks
]
